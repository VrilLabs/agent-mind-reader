aiohttp>=3.8
psutil>=5.9
pympler>=1.0
textual>=0.40
# optional: enables server-rendered PNG dashboards (SVG fallback if absent)
matplotlib>=3.6
# extract engine (deep-extract mind-read results; optional — /extract/run degrades gracefully if absent)
requests>=2.28
pyyaml>=6.0
